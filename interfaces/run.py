import subprocess

def run_interface():
    pass
    # run the npm command to start the interface
    # npm_command = ["npm", "start"]
    # subprocess.Popen(npm_command, cwd="interfaces/front-end")



    # # Run the Python command to start another python program
    # python_command = ["python", "interfaces/streamToInterface.py"]
    # subprocess.Popen(python_command)